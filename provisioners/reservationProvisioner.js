import { createOrGetReservationBusiness,createOrUpdateBusinessProfile,createOrUpdateRestaurantSettings,createOrUpdateCanonicalRestaurantService,activateCanonicalBookingModelIfEmpty,createOrUpdateRestaurantBranding,findReservationBusinessBySlug,getReservationProvisioningRecords } from "../utils/reservationService.js";
import { logReservationsOperation } from "../utils/reservationsOperationalLog.js";
import { getReservationsTemplate } from "../config/reservationsTemplates.js";
import { applyReservationsTemplate } from "../utils/reservationTemplateService.js";
import ChatbotSettings from "../models/chatbotSettings.js";
export const reservationProvisioningStore={createOrGetReservationBusiness,createOrUpdateBusinessProfile,createOrUpdateRestaurantSettings,createOrUpdateCanonicalRestaurantService,activateCanonicalBookingModelIfEmpty,createOrUpdateRestaurantBranding,findReservationBusinessBySlug,getReservationProvisioningRecords,applyReservationsTemplate};
const normalizeSlug=value=>typeof value==="string"&&value.trim()?value.trim().toLowerCase():null;const normalizeBusinessId=value=>{const id=Number(value);return Number.isFinite(id)&&id>0?id:null};const getChatbotReservationSlug=chatbot=>normalizeSlug(chatbot?.reservationBusinessSlug||chatbot?.reservationSlug);
export async function getReservationsProvisioningHealth({company,store=reservationProvisioningStore}){if(!company?._id)throw new Error("A company is required.");const companySlug=normalizeSlug(company.reservationBusinessSlug),companyBusinessId=normalizeBusinessId(company.reservationBusinessId),missing=[],mismatches=[];let business=null,profile=null,settings=null,branding=null,service=null;if(!companyBusinessId)missing.push("company.reservationBusinessId");if(!companySlug)missing.push("company.reservationBusinessSlug");else{business=await store.findReservationBusinessBySlug(companySlug);if(!business)missing.push("businesses");else{const resolvedBusinessId=normalizeBusinessId(business.id);if(companyBusinessId&&resolvedBusinessId&&companyBusinessId!==resolvedBusinessId)mismatches.push("company.reservationBusinessId");({profile,settings,branding,service}=await store.getReservationProvisioningRecords(business.id));if(!profile)missing.push("business_profile");if(!settings)missing.push("restaurant_settings");if(!branding)missing.push("restaurant_branding");if(!service)missing.push("services.restaurant");if(Number(business.booking_model_version||1)<2)missing.push("businesses.booking_model_version")}}const chatbot=await ChatbotSettings.findOne({companyId:company._id}),chatbotSlug=getChatbotReservationSlug(chatbot);let chatbotSlugResolves=false;if(chatbot&&chatbotSlug){if(chatbotSlug===companySlug)chatbotSlugResolves=Boolean(business);else{chatbotSlugResolves=Boolean(await store.findReservationBusinessBySlug(chatbotSlug));if(!chatbotSlugResolves)mismatches.push("chatbotSettings.reservationBusinessSlug")}}else if(chatbot)mismatches.push("chatbotSettings.reservationBusinessSlug");return{healthy:missing.length===0&&mismatches.length===0,companySlug,companyBusinessId,chatbotSlug,missing,mismatches,businessId:business?.id||null,chatbotSlugResolves}}
async function reconcileChatbotReservationSlug({company,business,store}){const chatbot=await ChatbotSettings.findOne({companyId:company._id});if(!chatbot)return{changed:false,reason:"no-chatbot-settings"};const canonicalSlug=normalizeSlug(company.reservationBusinessSlug),currentSlug=getChatbotReservationSlug(chatbot);if(currentSlug===canonicalSlug)return{changed:false,reason:"already-canonical"};if(currentSlug&&await store.findReservationBusinessBySlug(currentSlug))return{changed:false,reason:"valid-existing-link"};if(!business||!canonicalSlug)return{changed:false,reason:"canonical-business-unavailable"};if(chatbot.reservationBusinessSlug!==undefined||chatbot.schema?.path?.("reservationBusinessSlug"))chatbot.reservationBusinessSlug=canonicalSlug;else chatbot.reservationSlug=canonicalSlug;await chatbot.save();return{changed:true,reason:"stale-link-repaired"}}
export default async function provisionReservations({company,store=reservationProvisioningStore}){const templateKey=company.reservationTemplate||"general",template=getReservationsTemplate(templateKey);const business=await store.createOrGetReservationBusiness({businessName:company.displayName||company.name,businessSlug:company.reservationBusinessSlug,businessType:template.businessType});const profile=await store.createOrUpdateBusinessProfile({businessId:business.id,businessName:company.displayName||company.name,businessType:template.businessType,referencePrefix:company.referencePrefix});const settings=await store.createOrUpdateRestaurantSettings({businessId:business.id});const service=await store.createOrUpdateCanonicalRestaurantService({businessId:business.id});const bookingModel=await store.activateCanonicalBookingModelIfEmpty({businessId:business.id});const branding=await store.createOrUpdateRestaurantBranding({businessId:business.id,restaurantName:company.displayName||company.name});const customerForm=await store.applyReservationsTemplate({businessId:business.id,templateKey});const chatbotLink=await reconcileChatbotReservationSlug({company,business,store});if(!company.reservationBusinessId||Number(company.reservationBusinessId)!==Number(business.id)){company.reservationBusinessId=Number(business.id);await company.save()}logReservationsOperation("provisioning.completed",{companyId:company._id,businessId:business.id,businessSlug:business.business_slug,reservationTemplate:templateKey,bookingModelVersion:bookingModel?.business?.booking_model_version||1});return{success:true,business,profile,settings,service,bookingModel,branding,customerForm,chatbotLink}}
